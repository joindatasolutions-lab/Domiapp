# DomiApp - Domiciliarios (Mock)

Mobile-first React + TypeScript template for a delivery driver app. Built with Vite, Tailwind, React Query and Zustand. Designed for quick hand-operated flows.

Run locally:

```bash
npm install
npm run dev
```
